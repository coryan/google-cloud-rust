// Copyright 2024 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

use crate::Result;
use std::sync::Arc;

/// An implementation of [crate::traits::Compliance] to make requests with.
///
/// `Compliance` has various configuration parameters, but the defaults
/// are set to work with most applications.
///
/// `Compliance` holds a connection pool internally, it is advised to
/// create one and the reuse it.  You do not need to wrap `Compliance` in
/// an [Rc](std::rc::Rc) or [Arc] to reuse it, because it already uses an `Arc`
/// internally.
///
/// This service is used to test that GAPICs implement various REST-related features correctly. This mostly means transcoding proto3 requests to REST format
/// correctly for various types of HTTP annotations, but it also includes verifying that unknown (numeric) enums received by clients can be round-tripped
/// correctly.
#[derive(Clone, Debug)]
pub struct Compliance {
    inner: Arc<dyn crate::traits::dyntraits::Compliance>,
}

impl Compliance {
    /// Creates a new client with the default configuration.
    pub async fn new() -> Result<Self> {
        Self::new_with_config(crate::ConfigBuilder::default()).await
    }

    /// Creates a new client with the specified configuration.
    pub async fn new_with_config(conf: crate::ConfigBuilder) -> Result<Self> {
        let inner = Self::build_inner(conf).await?;
        Ok(Self { inner })
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is when mocking the
    /// client.
    pub fn from_stub<T>(stub: T) -> Self
    where
        T: crate::traits::Compliance + 'static,
    {
        Self {
            inner: Arc::new(stub),
        }
    }

    async fn build_inner(
        conf: crate::ConfigBuilder,
    ) -> Result<Arc<dyn crate::traits::dyntraits::Compliance>> {
        if conf.tracing_enabled() {
            return Ok(Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(conf: crate::ConfigBuilder) -> Result<impl crate::traits::Compliance> {
        crate::transport::Compliance::new(conf).await
    }

    async fn build_with_tracing(
        conf: crate::ConfigBuilder,
    ) -> Result<impl crate::traits::Compliance> {
        Self::build_transport(conf)
            .await
            .map(crate::tracing::Compliance::new)
    }

    /// This method echoes the ComplianceData request. This method exercises
    /// sending the entire request object in the REST body.
    pub fn repeat_data_body(&self) -> crate::builders::RepeatDataBody {
        crate::builders::RepeatDataBody::new(self.inner.clone())
    }

    /// This method echoes the ComplianceData request. This method exercises
    /// sending the a message-type field in the REST body. Per AIP-127, only
    /// top-level, non-repeated fields can be sent this way.
    pub fn repeat_data_body_info(&self) -> crate::builders::RepeatDataBodyInfo {
        crate::builders::RepeatDataBodyInfo::new(self.inner.clone())
    }

    /// This method echoes the ComplianceData request. This method exercises
    /// sending all request fields as query parameters.
    pub fn repeat_data_query(&self) -> crate::builders::RepeatDataQuery {
        crate::builders::RepeatDataQuery::new(self.inner.clone())
    }

    /// This method echoes the ComplianceData request. This method exercises
    /// sending some parameters as "simple" path variables (i.e., of the form
    /// "/bar/{foo}" rather than "/{foo=bar/*}"), and the rest as query parameters.
    pub fn repeat_data_simple_path(&self) -> crate::builders::RepeatDataSimplePath {
        crate::builders::RepeatDataSimplePath::new(self.inner.clone())
    }

    /// Same as RepeatDataSimplePath, but with a path resource.
    pub fn repeat_data_path_resource(&self) -> crate::builders::RepeatDataPathResource {
        crate::builders::RepeatDataPathResource::new(self.inner.clone())
    }

    /// Same as RepeatDataSimplePath, but with a trailing resource.
    pub fn repeat_data_path_trailing_resource(
        &self,
    ) -> crate::builders::RepeatDataPathTrailingResource {
        crate::builders::RepeatDataPathTrailingResource::new(self.inner.clone())
    }

    /// This method echoes the ComplianceData request, using the HTTP PUT method.
    pub fn repeat_data_body_put(&self) -> crate::builders::RepeatDataBodyPut {
        crate::builders::RepeatDataBodyPut::new(self.inner.clone())
    }

    /// This method echoes the ComplianceData request, using the HTTP PATCH method.
    pub fn repeat_data_body_patch(&self) -> crate::builders::RepeatDataBodyPatch {
        crate::builders::RepeatDataBodyPatch::new(self.inner.clone())
    }

    /// This method requests an enum value from the server. Depending on the contents of EnumRequest, the enum value returned will be a known enum declared in the
    /// .proto file, or a made-up enum value the is unknown to the client. To verify that clients can round-trip unknown enum vaues they receive, use the
    /// response from this RPC as the request to VerifyEnum()
    ///
    /// The values of enums sent by the server when a known or unknown value is requested will be the same within a single Showcase server run (this is needed for
    /// VerifyEnum() to work) but are not guaranteed to be the same across separate Showcase server runs.
    pub fn get_enum(&self) -> crate::builders::GetEnum {
        crate::builders::GetEnum::new(self.inner.clone())
    }

    /// This method is used to verify that clients can round-trip enum values, which is particularly important for unknown enum values over REST. VerifyEnum()
    /// verifies that its request, which is presumably the response that the client previously got to a GetEnum(), contains the correct data. If so, it responds
    /// with the same EnumResponse; otherwise, the RPC errors.
    ///
    /// This works because the values of enums sent by the server when a known or unknown value is requested will be the same within a single Showcase server run,
    /// although they are not guaranteed to be the same across separate Showcase server runs.
    pub fn verify_enum(&self) -> crate::builders::VerifyEnum {
        crate::builders::VerifyEnum::new(self.inner.clone())
    }
}

/// An implementation of [crate::traits::Echo] to make requests with.
///
/// `Echo` has various configuration parameters, but the defaults
/// are set to work with most applications.
///
/// `Echo` holds a connection pool internally, it is advised to
/// create one and the reuse it.  You do not need to wrap `Echo` in
/// an [Rc](std::rc::Rc) or [Arc] to reuse it, because it already uses an `Arc`
/// internally.
///
/// This service is used showcase the four main types of rpcs - unary, server
/// side streaming, client side streaming, and bidirectional streaming. This
/// service also exposes methods that explicitly implement server delay, and
/// paginated calls. Set the 'showcase-trailer' metadata key on any method
/// to have the values echoed in the response trailers. Set the
/// 'x-goog-request-params' metadata key on any method to have the values
/// echoed in the response headers.
#[derive(Clone, Debug)]
pub struct Echo {
    inner: Arc<dyn crate::traits::dyntraits::Echo>,
}

impl Echo {
    /// Creates a new client with the default configuration.
    pub async fn new() -> Result<Self> {
        Self::new_with_config(crate::ConfigBuilder::default()).await
    }

    /// Creates a new client with the specified configuration.
    pub async fn new_with_config(conf: crate::ConfigBuilder) -> Result<Self> {
        let inner = Self::build_inner(conf).await?;
        Ok(Self { inner })
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is when mocking the
    /// client.
    pub fn from_stub<T>(stub: T) -> Self
    where
        T: crate::traits::Echo + 'static,
    {
        Self {
            inner: Arc::new(stub),
        }
    }

    async fn build_inner(
        conf: crate::ConfigBuilder,
    ) -> Result<Arc<dyn crate::traits::dyntraits::Echo>> {
        if conf.tracing_enabled() {
            return Ok(Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(conf: crate::ConfigBuilder) -> Result<impl crate::traits::Echo> {
        crate::transport::Echo::new(conf).await
    }

    async fn build_with_tracing(conf: crate::ConfigBuilder) -> Result<impl crate::traits::Echo> {
        Self::build_transport(conf)
            .await
            .map(crate::tracing::Echo::new)
    }

    /// This method simply echoes the request. This method showcases unary RPCs.
    pub fn echo(&self) -> crate::builders::Echo {
        crate::builders::Echo::new(self.inner.clone())
    }

    /// This method returns error details in a repeated "google.protobuf.Any"
    /// field. This method showcases handling errors thus encoded, particularly
    /// over REST transport. Note that GAPICs only allow the type
    /// "google.protobuf.Any" for field paths ending in "error.details", and, at
    /// run-time, the actual types for these fields must be one of the types in
    /// google/rpc/error_details.proto.
    pub fn echo_error_details(&self) -> crate::builders::EchoErrorDetails {
        crate::builders::EchoErrorDetails::new(self.inner.clone())
    }

    /// This is similar to the Expand method but instead of returning a stream of
    /// expanded words, this method returns a paged list of expanded words.
    pub fn paged_expand(&self) -> crate::builders::PagedExpand {
        crate::builders::PagedExpand::new(self.inner.clone())
    }

    /// This is similar to the PagedExpand except that it uses
    /// max_results instead of page_size, as some legacy APIs still
    /// do. New APIs should NOT use this pattern.
    pub fn paged_expand_legacy(&self) -> crate::builders::PagedExpandLegacy {
        crate::builders::PagedExpandLegacy::new(self.inner.clone())
    }

    /// This method returns a map containing lists of words that appear in the input, keyed by their
    /// initial character. The only words returned are the ones included in the current page,
    /// as determined by page_token and page_size, which both refer to the word indices in the
    /// input. This paging result consisting of a map of lists is a pattern used by some legacy
    /// APIs. New APIs should NOT use this pattern.
    pub fn paged_expand_legacy_mapped(&self) -> crate::builders::PagedExpandLegacyMapped {
        crate::builders::PagedExpandLegacyMapped::new(self.inner.clone())
    }

    /// This method will wait for the requested amount of time and then return.
    /// This method showcases how a client handles a request timeout.
    pub fn wait(&self) -> crate::builders::Wait {
        crate::builders::Wait::new(self.inner.clone())
    }

    /// This method will block (wait) for the requested amount of time
    /// and then return the response or error.
    /// This method showcases how a client handles delays or retries.
    pub fn block(&self) -> crate::builders::Block {
        crate::builders::Block::new(self.inner.clone())
    }
}

/// An implementation of [crate::traits::Identity] to make requests with.
///
/// `Identity` has various configuration parameters, but the defaults
/// are set to work with most applications.
///
/// `Identity` holds a connection pool internally, it is advised to
/// create one and the reuse it.  You do not need to wrap `Identity` in
/// an [Rc](std::rc::Rc) or [Arc] to reuse it, because it already uses an `Arc`
/// internally.
///
/// A simple identity service.
#[derive(Clone, Debug)]
pub struct Identity {
    inner: Arc<dyn crate::traits::dyntraits::Identity>,
}

impl Identity {
    /// Creates a new client with the default configuration.
    pub async fn new() -> Result<Self> {
        Self::new_with_config(crate::ConfigBuilder::default()).await
    }

    /// Creates a new client with the specified configuration.
    pub async fn new_with_config(conf: crate::ConfigBuilder) -> Result<Self> {
        let inner = Self::build_inner(conf).await?;
        Ok(Self { inner })
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is when mocking the
    /// client.
    pub fn from_stub<T>(stub: T) -> Self
    where
        T: crate::traits::Identity + 'static,
    {
        Self {
            inner: Arc::new(stub),
        }
    }

    async fn build_inner(
        conf: crate::ConfigBuilder,
    ) -> Result<Arc<dyn crate::traits::dyntraits::Identity>> {
        if conf.tracing_enabled() {
            return Ok(Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(conf: crate::ConfigBuilder) -> Result<impl crate::traits::Identity> {
        crate::transport::Identity::new(conf).await
    }

    async fn build_with_tracing(
        conf: crate::ConfigBuilder,
    ) -> Result<impl crate::traits::Identity> {
        Self::build_transport(conf)
            .await
            .map(crate::tracing::Identity::new)
    }

    /// Creates a user.
    pub fn create_user(&self) -> crate::builders::CreateUser {
        crate::builders::CreateUser::new(self.inner.clone())
    }

    /// Retrieves the User with the given uri.
    pub fn get_user(&self) -> crate::builders::GetUser {
        crate::builders::GetUser::new(self.inner.clone())
    }

    /// Updates a user.
    pub fn update_user(&self) -> crate::builders::UpdateUser {
        crate::builders::UpdateUser::new(self.inner.clone())
    }

    /// Deletes a user, their profile, and all of their authored messages.
    pub fn delete_user(&self) -> crate::builders::DeleteUser {
        crate::builders::DeleteUser::new(self.inner.clone())
    }

    /// Lists all users.
    pub fn list_users(&self) -> crate::builders::ListUsers {
        crate::builders::ListUsers::new(self.inner.clone())
    }
}

/// An implementation of [crate::traits::Messaging] to make requests with.
///
/// `Messaging` has various configuration parameters, but the defaults
/// are set to work with most applications.
///
/// `Messaging` holds a connection pool internally, it is advised to
/// create one and the reuse it.  You do not need to wrap `Messaging` in
/// an [Rc](std::rc::Rc) or [Arc] to reuse it, because it already uses an `Arc`
/// internally.
///
/// A simple messaging service that implements chat rooms and profile posts.
///
/// This messaging service showcases the features that API clients
/// generated by gapic-generators implement.
#[derive(Clone, Debug)]
pub struct Messaging {
    inner: Arc<dyn crate::traits::dyntraits::Messaging>,
}

impl Messaging {
    /// Creates a new client with the default configuration.
    pub async fn new() -> Result<Self> {
        Self::new_with_config(crate::ConfigBuilder::default()).await
    }

    /// Creates a new client with the specified configuration.
    pub async fn new_with_config(conf: crate::ConfigBuilder) -> Result<Self> {
        let inner = Self::build_inner(conf).await?;
        Ok(Self { inner })
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is when mocking the
    /// client.
    pub fn from_stub<T>(stub: T) -> Self
    where
        T: crate::traits::Messaging + 'static,
    {
        Self {
            inner: Arc::new(stub),
        }
    }

    async fn build_inner(
        conf: crate::ConfigBuilder,
    ) -> Result<Arc<dyn crate::traits::dyntraits::Messaging>> {
        if conf.tracing_enabled() {
            return Ok(Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(conf: crate::ConfigBuilder) -> Result<impl crate::traits::Messaging> {
        crate::transport::Messaging::new(conf).await
    }

    async fn build_with_tracing(
        conf: crate::ConfigBuilder,
    ) -> Result<impl crate::traits::Messaging> {
        Self::build_transport(conf)
            .await
            .map(crate::tracing::Messaging::new)
    }

    /// Creates a room.
    pub fn create_room(&self) -> crate::builders::CreateRoom {
        crate::builders::CreateRoom::new(self.inner.clone())
    }

    /// Retrieves the Room with the given resource name.
    pub fn get_room(&self) -> crate::builders::GetRoom {
        crate::builders::GetRoom::new(self.inner.clone())
    }

    /// Updates a room.
    pub fn update_room(&self) -> crate::builders::UpdateRoom {
        crate::builders::UpdateRoom::new(self.inner.clone())
    }

    /// Deletes a room and all of its blurbs.
    pub fn delete_room(&self) -> crate::builders::DeleteRoom {
        crate::builders::DeleteRoom::new(self.inner.clone())
    }

    /// Lists all chat rooms.
    pub fn list_rooms(&self) -> crate::builders::ListRooms {
        crate::builders::ListRooms::new(self.inner.clone())
    }

    /// Creates a blurb. If the parent is a room, the blurb is understood to be a
    /// message in that room. If the parent is a profile, the blurb is understood
    /// to be a post on the profile.
    pub fn create_blurb(&self) -> crate::builders::CreateBlurb {
        crate::builders::CreateBlurb::new(self.inner.clone())
    }

    /// Retrieves the Blurb with the given resource name.
    pub fn get_blurb(&self) -> crate::builders::GetBlurb {
        crate::builders::GetBlurb::new(self.inner.clone())
    }

    /// Updates a blurb.
    pub fn update_blurb(&self) -> crate::builders::UpdateBlurb {
        crate::builders::UpdateBlurb::new(self.inner.clone())
    }

    /// Deletes a blurb.
    pub fn delete_blurb(&self) -> crate::builders::DeleteBlurb {
        crate::builders::DeleteBlurb::new(self.inner.clone())
    }

    /// Lists blurbs for a specific chat room or user profile depending on the
    /// parent resource name.
    pub fn list_blurbs(&self) -> crate::builders::ListBlurbs {
        crate::builders::ListBlurbs::new(self.inner.clone())
    }

    /// This method searches through all blurbs across all rooms and profiles
    /// for blurbs containing to words found in the query. Only posts that
    /// contain an exact match of a queried word will be returned.
    pub fn search_blurbs(&self) -> crate::builders::SearchBlurbs {
        crate::builders::SearchBlurbs::new(self.inner.clone())
    }
}

/// An implementation of [crate::traits::SequenceService] to make requests with.
///
/// `SequenceService` has various configuration parameters, but the defaults
/// are set to work with most applications.
///
/// `SequenceService` holds a connection pool internally, it is advised to
/// create one and the reuse it.  You do not need to wrap `SequenceService` in
/// an [Rc](std::rc::Rc) or [Arc] to reuse it, because it already uses an `Arc`
/// internally.
///
///
#[derive(Clone, Debug)]
pub struct SequenceService {
    inner: Arc<dyn crate::traits::dyntraits::SequenceService>,
}

impl SequenceService {
    /// Creates a new client with the default configuration.
    pub async fn new() -> Result<Self> {
        Self::new_with_config(crate::ConfigBuilder::default()).await
    }

    /// Creates a new client with the specified configuration.
    pub async fn new_with_config(conf: crate::ConfigBuilder) -> Result<Self> {
        let inner = Self::build_inner(conf).await?;
        Ok(Self { inner })
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is when mocking the
    /// client.
    pub fn from_stub<T>(stub: T) -> Self
    where
        T: crate::traits::SequenceService + 'static,
    {
        Self {
            inner: Arc::new(stub),
        }
    }

    async fn build_inner(
        conf: crate::ConfigBuilder,
    ) -> Result<Arc<dyn crate::traits::dyntraits::SequenceService>> {
        if conf.tracing_enabled() {
            return Ok(Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(
        conf: crate::ConfigBuilder,
    ) -> Result<impl crate::traits::SequenceService> {
        crate::transport::SequenceService::new(conf).await
    }

    async fn build_with_tracing(
        conf: crate::ConfigBuilder,
    ) -> Result<impl crate::traits::SequenceService> {
        Self::build_transport(conf)
            .await
            .map(crate::tracing::SequenceService::new)
    }

    /// Creates a sequence.
    pub fn create_sequence(&self) -> crate::builders::CreateSequence {
        crate::builders::CreateSequence::new(self.inner.clone())
    }

    /// Creates a sequence.
    pub fn create_streaming_sequence(&self) -> crate::builders::CreateStreamingSequence {
        crate::builders::CreateStreamingSequence::new(self.inner.clone())
    }

    /// Retrieves a sequence.
    pub fn get_sequence_report(&self) -> crate::builders::GetSequenceReport {
        crate::builders::GetSequenceReport::new(self.inner.clone())
    }

    /// Retrieves a sequence.
    pub fn get_streaming_sequence_report(&self) -> crate::builders::GetStreamingSequenceReport {
        crate::builders::GetStreamingSequenceReport::new(self.inner.clone())
    }

    /// Attempts a sequence.
    pub fn attempt_sequence(&self) -> crate::builders::AttemptSequence {
        crate::builders::AttemptSequence::new(self.inner.clone())
    }
}

/// An implementation of [crate::traits::Testing] to make requests with.
///
/// `Testing` has various configuration parameters, but the defaults
/// are set to work with most applications.
///
/// `Testing` holds a connection pool internally, it is advised to
/// create one and the reuse it.  You do not need to wrap `Testing` in
/// an [Rc](std::rc::Rc) or [Arc] to reuse it, because it already uses an `Arc`
/// internally.
///
/// A service to facilitate running discrete sets of tests
/// against Showcase.
/// Adding this comment with special characters for comment formatting tests:
/// 1. (abra->kadabra->alakazam)
/// 2) [Nonsense][]: `pokemon/*/psychic/*`
#[derive(Clone, Debug)]
pub struct Testing {
    inner: Arc<dyn crate::traits::dyntraits::Testing>,
}

impl Testing {
    /// Creates a new client with the default configuration.
    pub async fn new() -> Result<Self> {
        Self::new_with_config(crate::ConfigBuilder::default()).await
    }

    /// Creates a new client with the specified configuration.
    pub async fn new_with_config(conf: crate::ConfigBuilder) -> Result<Self> {
        let inner = Self::build_inner(conf).await?;
        Ok(Self { inner })
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is when mocking the
    /// client.
    pub fn from_stub<T>(stub: T) -> Self
    where
        T: crate::traits::Testing + 'static,
    {
        Self {
            inner: Arc::new(stub),
        }
    }

    async fn build_inner(
        conf: crate::ConfigBuilder,
    ) -> Result<Arc<dyn crate::traits::dyntraits::Testing>> {
        if conf.tracing_enabled() {
            return Ok(Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(conf: crate::ConfigBuilder) -> Result<impl crate::traits::Testing> {
        crate::transport::Testing::new(conf).await
    }

    async fn build_with_tracing(conf: crate::ConfigBuilder) -> Result<impl crate::traits::Testing> {
        Self::build_transport(conf)
            .await
            .map(crate::tracing::Testing::new)
    }

    /// Creates a new testing session.
    /// Adding this comment with special characters for comment formatting tests:
    /// 1. (abra->kadabra->alakazam)
    /// 2) [Nonsense][]: `pokemon/*/psychic/*`
    pub fn create_session(&self) -> crate::builders::CreateSession {
        crate::builders::CreateSession::new(self.inner.clone())
    }

    /// Gets a testing session.
    pub fn get_session(&self) -> crate::builders::GetSession {
        crate::builders::GetSession::new(self.inner.clone())
    }

    /// Lists the current test sessions.
    pub fn list_sessions(&self) -> crate::builders::ListSessions {
        crate::builders::ListSessions::new(self.inner.clone())
    }

    /// Delete a test session.
    pub fn delete_session(&self) -> crate::builders::DeleteSession {
        crate::builders::DeleteSession::new(self.inner.clone())
    }

    /// Report on the status of a session.
    /// This generates a report detailing which tests have been completed,
    /// and an overall rollup.
    pub fn report_session(&self) -> crate::builders::ReportSession {
        crate::builders::ReportSession::new(self.inner.clone())
    }

    /// List the tests of a sessesion.
    pub fn list_tests(&self) -> crate::builders::ListTests {
        crate::builders::ListTests::new(self.inner.clone())
    }

    /// Explicitly decline to implement a test.
    ///
    /// This removes the test from subsequent `ListTests` calls, and
    /// attempting to do the test will error.
    ///
    /// This method will error if attempting to delete a required test.
    pub fn delete_test(&self) -> crate::builders::DeleteTest {
        crate::builders::DeleteTest::new(self.inner.clone())
    }

    /// Register a response to a test.
    ///
    /// In cases where a test involves registering a final answer at the
    /// end of the test, this method provides the means to do so.
    pub fn verify_test(&self) -> crate::builders::VerifyTest {
        crate::builders::VerifyTest::new(self.inner.clone())
    }
}

/// An implementation of [crate::traits::Locations] to make requests with.
///
/// `Locations` has various configuration parameters, but the defaults
/// are set to work with most applications.
///
/// `Locations` holds a connection pool internally, it is advised to
/// create one and the reuse it.  You do not need to wrap `Locations` in
/// an [Rc](std::rc::Rc) or [Arc] to reuse it, because it already uses an `Arc`
/// internally.
///
/// Manages location-related information with an API service.
#[derive(Clone, Debug)]
pub struct Locations {
    inner: Arc<dyn crate::traits::dyntraits::Locations>,
}

impl Locations {
    /// Creates a new client with the default configuration.
    pub async fn new() -> Result<Self> {
        Self::new_with_config(crate::ConfigBuilder::default()).await
    }

    /// Creates a new client with the specified configuration.
    pub async fn new_with_config(conf: crate::ConfigBuilder) -> Result<Self> {
        let inner = Self::build_inner(conf).await?;
        Ok(Self { inner })
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is when mocking the
    /// client.
    pub fn from_stub<T>(stub: T) -> Self
    where
        T: crate::traits::Locations + 'static,
    {
        Self {
            inner: Arc::new(stub),
        }
    }

    async fn build_inner(
        conf: crate::ConfigBuilder,
    ) -> Result<Arc<dyn crate::traits::dyntraits::Locations>> {
        if conf.tracing_enabled() {
            return Ok(Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(conf: crate::ConfigBuilder) -> Result<impl crate::traits::Locations> {
        crate::transport::Locations::new(conf).await
    }

    async fn build_with_tracing(
        conf: crate::ConfigBuilder,
    ) -> Result<impl crate::traits::Locations> {
        Self::build_transport(conf)
            .await
            .map(crate::tracing::Locations::new)
    }

    /// ListLocations is an RPC method of Locations.
    pub fn list_locations(&self) -> crate::builders::ListLocations {
        crate::builders::ListLocations::new(self.inner.clone())
    }

    /// GetLocation is an RPC method of Locations.
    pub fn get_location(&self) -> crate::builders::GetLocation {
        crate::builders::GetLocation::new(self.inner.clone())
    }
}

/// An implementation of [crate::traits::IAMPolicy] to make requests with.
///
/// `IAMPolicy` has various configuration parameters, but the defaults
/// are set to work with most applications.
///
/// `IAMPolicy` holds a connection pool internally, it is advised to
/// create one and the reuse it.  You do not need to wrap `IAMPolicy` in
/// an [Rc](std::rc::Rc) or [Arc] to reuse it, because it already uses an `Arc`
/// internally.
///
/// Manages Identity and Access Management (IAM) policies with an API service.
#[derive(Clone, Debug)]
pub struct IAMPolicy {
    inner: Arc<dyn crate::traits::dyntraits::IAMPolicy>,
}

impl IAMPolicy {
    /// Creates a new client with the default configuration.
    pub async fn new() -> Result<Self> {
        Self::new_with_config(crate::ConfigBuilder::default()).await
    }

    /// Creates a new client with the specified configuration.
    pub async fn new_with_config(conf: crate::ConfigBuilder) -> Result<Self> {
        let inner = Self::build_inner(conf).await?;
        Ok(Self { inner })
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is when mocking the
    /// client.
    pub fn from_stub<T>(stub: T) -> Self
    where
        T: crate::traits::IAMPolicy + 'static,
    {
        Self {
            inner: Arc::new(stub),
        }
    }

    async fn build_inner(
        conf: crate::ConfigBuilder,
    ) -> Result<Arc<dyn crate::traits::dyntraits::IAMPolicy>> {
        if conf.tracing_enabled() {
            return Ok(Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(conf: crate::ConfigBuilder) -> Result<impl crate::traits::IAMPolicy> {
        crate::transport::IAMPolicy::new(conf).await
    }

    async fn build_with_tracing(
        conf: crate::ConfigBuilder,
    ) -> Result<impl crate::traits::IAMPolicy> {
        Self::build_transport(conf)
            .await
            .map(crate::tracing::IAMPolicy::new)
    }

    /// SetIamPolicy is an RPC method of IAMPolicy.
    pub fn set_iam_policy(&self) -> crate::builders::SetIamPolicy {
        crate::builders::SetIamPolicy::new(self.inner.clone())
    }

    /// GetIamPolicy is an RPC method of IAMPolicy.
    pub fn get_iam_policy(&self) -> crate::builders::GetIamPolicy {
        crate::builders::GetIamPolicy::new(self.inner.clone())
    }

    /// TestIamPermissions is an RPC method of IAMPolicy.
    pub fn test_iam_permissions(&self) -> crate::builders::TestIamPermissions {
        crate::builders::TestIamPermissions::new(self.inner.clone())
    }
}

/// An implementation of [crate::traits::Operations] to make requests with.
///
/// `Operations` has various configuration parameters, but the defaults
/// are set to work with most applications.
///
/// `Operations` holds a connection pool internally, it is advised to
/// create one and the reuse it.  You do not need to wrap `Operations` in
/// an [Rc](std::rc::Rc) or [Arc] to reuse it, because it already uses an `Arc`
/// internally.
///
/// Manages long-running operations with an API service.
#[derive(Clone, Debug)]
pub struct Operations {
    inner: Arc<dyn crate::traits::dyntraits::Operations>,
}

impl Operations {
    /// Creates a new client with the default configuration.
    pub async fn new() -> Result<Self> {
        Self::new_with_config(crate::ConfigBuilder::default()).await
    }

    /// Creates a new client with the specified configuration.
    pub async fn new_with_config(conf: crate::ConfigBuilder) -> Result<Self> {
        let inner = Self::build_inner(conf).await?;
        Ok(Self { inner })
    }

    /// Creates a new client from the provided stub.
    ///
    /// The most common case for calling this function is when mocking the
    /// client.
    pub fn from_stub<T>(stub: T) -> Self
    where
        T: crate::traits::Operations + 'static,
    {
        Self {
            inner: Arc::new(stub),
        }
    }

    async fn build_inner(
        conf: crate::ConfigBuilder,
    ) -> Result<Arc<dyn crate::traits::dyntraits::Operations>> {
        if conf.tracing_enabled() {
            return Ok(Arc::new(Self::build_with_tracing(conf).await?));
        }
        Ok(Arc::new(Self::build_transport(conf).await?))
    }

    async fn build_transport(conf: crate::ConfigBuilder) -> Result<impl crate::traits::Operations> {
        crate::transport::Operations::new(conf).await
    }

    async fn build_with_tracing(
        conf: crate::ConfigBuilder,
    ) -> Result<impl crate::traits::Operations> {
        Self::build_transport(conf)
            .await
            .map(crate::tracing::Operations::new)
    }

    /// ListOperations is an RPC method of Operations.
    pub fn list_operations(&self) -> crate::builders::ListOperations {
        crate::builders::ListOperations::new(self.inner.clone())
    }

    /// GetOperation is an RPC method of Operations.
    pub fn get_operation(&self) -> crate::builders::GetOperation {
        crate::builders::GetOperation::new(self.inner.clone())
    }

    /// DeleteOperation is an RPC method of Operations.
    pub fn delete_operation(&self) -> crate::builders::DeleteOperation {
        crate::builders::DeleteOperation::new(self.inner.clone())
    }

    /// CancelOperation is an RPC method of Operations.
    pub fn cancel_operation(&self) -> crate::builders::CancelOperation {
        crate::builders::CancelOperation::new(self.inner.clone())
    }
}
